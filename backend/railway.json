{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py collectstatic --noinput && daphne neora.asgi:application --bind 0.0.0.0 --port $PORT",
    "healthcheckPath": "/api/"
  }
}
